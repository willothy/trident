[package]
name = "trident"
version = "0.1.0"
edition = "2021"

[dependencies]
ptree = "0.4.0"
rand = "0.8.5"

[dev-dependencies]
criterion = { version = "0.5.1", features = ["html_reports"] }
parity-wordlist = "1.3.1"

[[bench]]
name = "bench"
harness = false
